[pytest]
testpaths = backend/tests
# test_engine.py is a manual live-API script (python -m backend.tests.test_engine),
# not a unit test — exclude it from automated runs.
addopts = --ignore=backend/tests/test_engine.py -q
